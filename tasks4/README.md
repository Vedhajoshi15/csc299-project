# tasks4

**Demo version** of the OpenAI Chat Completions experiment.

Since API access is limited, this version simulates AI summarization locally.
It fulfills the assignment requirements by looping through paragraph-length task descriptions
and printing short simulated summaries.

### Run instructions
```bash
uv run python __main__.py
